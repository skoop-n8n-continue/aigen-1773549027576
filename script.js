// script.js
// Non-interactive digital signage script

document.addEventListener('DOMContentLoaded', () => {
    console.log('Digital Signage: Hello World initialized.');

    // Future dynamic responsive logic or animations could be initialized here.
    // Since everything is responsive via CSS (flexbox and vw units), JS is minimal.
});